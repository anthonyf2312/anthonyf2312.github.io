# Secrets: the Pixabay key lives here and never leaves this machine
.env
.env.*

# Build output and dependencies
node_modules/
dist/
.astro/

# Impeccable working files (screenshots, mocks, decision page payloads)
.impeccable/review/
.impeccable/mocks/
.impeccable/decision/
.impeccable/questions/
.impeccable/hook.cache.json
.impeccable/config.local.json

# Blender's automatic backup of the brand file
*.blend1

# OS
.DS_Store
Thumbs.db
