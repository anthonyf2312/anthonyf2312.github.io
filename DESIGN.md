---
name: Anthony
description: A personal page held to the standard of an Apple product page, with Stripe's polish.
colors:
  white: "#ffffff"
  grey: "#f5f5f7"
  black: "#000000"
  ink: "#1d1d1f"
  ink-2: "#6e6e73"
  on-dark: "#f5f5f7"
  on-dark-2: "#a1a1a6"
  hair: "#d2d2d7"
  hair-dark: "#2c2c2e"
  tile: "#161617"
  lando-volt: "#d2ff00"
  papaya: "#ff8000"
  papaya-text: "#a94f00"
  papaya-on-dark: "#ff9a3d"
  ribbon: "#f7862b"
  # Patchr site (/patchr/): a separate world. patchr- tokens apply only to src/pages/patchr/, the Patchr layouts, src/components/patchr/ and src/styles/patchr.css.
  patchr-deep: "#0e0c1a"
  patchr-ink: "#17142b"
  patchr-mist: "#f4f2fa"
  patchr-white: "#ffffff"
  patchr-haze: "#bdb7d9"
  patchr-muted: "#6b6785"
  patchr-line-light: "#e2deef"
  patchr-line-dark: "rgb(189 183 217 / 0.14)"
  patchr-pink: "#ff5e8a"
  patchr-pink-hover: "#ff779c"
  patchr-pink-text: "#c81e57"
  patchr-pink-soft: "#ffb3c7"
  patchr-mint: "#3ddc97"
  patchr-lilac: "#8b7cff"
  patchr-dc-bg: "#313338"
  patchr-dc-surface: "#2b2d31"
  patchr-dc-deep: "#1e1f22"
  patchr-dc-text: "#dbdee1"
  patchr-dc-head: "#f2f3f5"
  patchr-dc-muted: "#949ba4"
  patchr-dc-link: "#00a8fc"
  patchr-dc-blurple: "#5865f2"
  patchr-dc-sep: "#3f4147"
  patchr-dc-button: "#4e5058"
typography:
  display:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(3.25rem, 10vw, 6rem)"
    fontWeight: 650
    lineHeight: 1
    letterSpacing: "-0.04em"
  headline:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(2.5rem, 6vw, 4.5rem)"
    fontWeight: 650
    lineHeight: 1.05
    letterSpacing: "-0.035em"
  statement:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(2rem, 4.6vw, 3.5rem)"
    fontWeight: 650
    lineHeight: 1.12
    letterSpacing: "-0.035em"
  title:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(1.375rem, 2.4vw, 1.75rem)"
    fontWeight: 650
    letterSpacing: "-0.025em"
  lead:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(1.1875rem, 1.9vw, 1.5rem)"
    fontWeight: 500
    lineHeight: 1.4
    letterSpacing: "-0.012em"
  body:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(1.0625rem, 1.3vw, 1.1875rem)"
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "-0.005em"
  label:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "0.875rem"
    fontWeight: 400
    lineHeight: 1
  fine:
    fontFamily: "'Mona Sans Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "0.8125rem"
    fontWeight: 400
    lineHeight: 1.45
  # Patchr site (/patchr/): a separate world. patchr- tokens apply only to src/pages/patchr/, the Patchr layouts, src/components/patchr/ and src/styles/patchr.css.
  patchr-display:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(2.875rem, 6.4vw, 5.25rem)"
    fontWeight: 800
    lineHeight: 0.98
    letterSpacing: "-0.04em"
  patchr-figure:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(3.5rem, 8vw, 6.5rem)"
    fontWeight: 800
    lineHeight: 0.95
    letterSpacing: "-0.04em"
    fontFeature: '"tnum"'
  patchr-headline:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(2.25rem, 4.8vw, 3.875rem)"
    fontWeight: 800
    lineHeight: 1.04
    letterSpacing: "-0.035em"
  patchr-title:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(1.25rem, 2vw, 1.5rem)"
    fontWeight: 700
    lineHeight: 1.25
    letterSpacing: "-0.022em"
  patchr-lead:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "clamp(1.125rem, 1.65vw, 1.375rem)"
    fontWeight: 500
    lineHeight: 1.45
    letterSpacing: "-0.016em"
  patchr-body:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "1.0625rem"
    fontWeight: 400
    lineHeight: 1.55
    letterSpacing: "-0.011em"
  patchr-label:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "0.9375rem"
    fontWeight: 500
  patchr-button:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "1rem"
    fontWeight: 650
    lineHeight: 1
    letterSpacing: "-0.012em"
  patchr-fine:
    fontFamily: "'Inter Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "0.8125rem"
    fontWeight: 400
    lineHeight: 1.5
  patchr-mono:
    fontFamily: "ui-monospace, 'SF Mono', 'Cascadia Mono', Consolas, monospace"
    fontSize: "0.9em"
    fontWeight: 500
    letterSpacing: "0"
rounded:
  media: "28px"
  pill: "980px"
  focus: "6px"
  # Patchr site (/patchr/): a separate world. patchr- tokens apply only to src/pages/patchr/, the Patchr layouts, src/components/patchr/ and src/styles/patchr.css.
  patchr-screen: "24px"
  patchr-pill: "980px"
  patchr-panel: "16px"
  patchr-panel-item: "10px"
  patchr-focus: "8px"
  patchr-chip: "6px"
spacing:
  s1: "0.5rem"
  s2: "1rem"
  s3: "1.5rem"
  s4: "2rem"
  s6: "3rem"
  s8: "4rem"
  s12: "6rem"
  s16: "8rem"
  gutter: "clamp(1.25rem, 5vw, 3rem)"
  section-y: "clamp(6rem, 13vw, 10rem)"
  container: "1080px"
  # Patchr site (/patchr/): a separate world. patchr- tokens apply only to src/pages/patchr/, the Patchr layouts, src/components/patchr/ and src/styles/patchr.css.
  patchr-container: "1120px"
  patchr-section-y: "clamp(5.5rem, 12vw, 9.5rem)"
  patchr-nav-h: "3.75rem"
  patchr-text-column: "27rem"
components:
  button-pill:
    backgroundColor: "{colors.papaya}"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "0 1.375rem"
    height: "2.75rem"
  text-link:
    textColor: "{colors.papaya-text}"
    height: "2.75rem"
  text-link-on-dark:
    textColor: "{colors.papaya-on-dark}"
    height: "2.75rem"
  nav:
    textColor: "{colors.ink}"
    height: "3rem"
  nav-dark:
    textColor: "{colors.on-dark}"
    height: "3rem"
  media-frame:
    rounded: "{rounded.media} on the inner corners only; the bled edge is square"
    width: "from its grid line to the viewport edge"
  hairline-list-item:
    textColor: "{colors.ink}"
    padding: "1.5rem 0"
  # Patchr site (/patchr/): a separate world. patchr- tokens apply only to src/pages/patchr/, the Patchr layouts, src/components/patchr/ and src/styles/patchr.css.
  patchr-button-pink:
    backgroundColor: "{colors.patchr-pink}"
    textColor: "{colors.patchr-ink}"
    typography: "{typography.patchr-button}"
    rounded: "{rounded.patchr-pill}"
    padding: "0 1.375rem"
    height: "3rem"
  patchr-button-pink-hover:
    backgroundColor: "{colors.patchr-pink-hover}"
  patchr-button-ink-ghost:
    textColor: "{colors.patchr-ink}"
    typography: "{typography.patchr-button}"
    rounded: "{rounded.patchr-pill}"
    padding: "0 1.375rem"
    height: "3rem"
  patchr-button-ghost:
    textColor: "{colors.patchr-white}"
    typography: "{typography.patchr-button}"
    rounded: "{rounded.patchr-pill}"
    padding: "0 1.375rem"
    height: "3rem"
  patchr-button-ghost-light:
    textColor: "{colors.patchr-ink}"
  patchr-button-ink:
    backgroundColor: "{colors.patchr-ink}"
    textColor: "{colors.patchr-white}"
    typography: "{typography.patchr-button}"
    rounded: "{rounded.patchr-pill}"
    padding: "0 1.375rem"
    height: "3rem"
  patchr-button-ink-hover:
    backgroundColor: "#2a2546"
  patchr-button-sm:
    padding: "0 1rem"
    height: "2.5rem"
  patchr-text-link:
    textColor: "{colors.patchr-pink}"
    height: "2.75rem"
  patchr-text-link-light:
    textColor: "{colors.patchr-pink-text}"
    height: "2.75rem"
  patchr-command-chip:
    backgroundColor: "rgb(255 94 138 / 0.14)"
    textColor: "{colors.patchr-pink-soft}"
    typography: "{typography.patchr-mono}"
    rounded: "{rounded.patchr-chip}"
    padding: "0.1em 0.45em"
  patchr-command-chip-light:
    backgroundColor: "rgb(200 30 87 / 0.08)"
    textColor: "{colors.patchr-pink-text}"
  patchr-nav:
    backgroundColor: "rgb(14 12 26 / 0.72)"
    textColor: "{colors.patchr-white}"
    height: "{spacing.patchr-nav-h}"
  patchr-nav-light:
    backgroundColor: "rgb(244 242 250 / 0.78)"
    textColor: "{colors.patchr-ink}"
    height: "{spacing.patchr-nav-h}"
  patchr-nav-menu:
    backgroundColor: "{colors.patchr-ink}"
    textColor: "{colors.patchr-white}"
    rounded: "{rounded.patchr-panel}"
    padding: "0.5rem"
  patchr-discord-screen:
    backgroundColor: "{colors.patchr-dc-bg}"
    textColor: "{colors.patchr-dc-text}"
    rounded: "{rounded.patchr-screen} on the inner corners only; the bled edge is square"
    width: "from its grid line to the viewport edge"
  patchr-patch-card:
    backgroundColor: "{colors.patchr-dc-surface}"
    textColor: "{colors.patchr-dc-text}"
    rounded: "0.5em"
    padding: "1em 1em 1em 1.25em"
    width: "max 32em"
  patchr-spec-row:
    textColor: "{colors.patchr-ink}"
    padding: "2rem 0"
  patchr-cta-band:
    backgroundColor: "{colors.patchr-pink}"
    textColor: "{colors.patchr-ink}"
    padding: "clamp(5rem, 11vw, 8.5rem) 0"
---

# Design System: Anthony

## Overview

**Creative North Star: "The Product Page"**

A personal page built the way Apple builds a product page, with Stripe's finish: one idea per section, big confident type, real media, and scroll moments that carry the story. Every section is a full-width band on one of three grounds (white, Apple grey, true black), with content centred inside a 1080px column. Density is low on purpose. A section holds a headline, a lead, and at most one piece of media or one pair of actions.

Papaya is the only colour that isn't a neutral. It appears as the pill button, the text links, the drawn lines (the Sochi trace, the MS wave), the hero's drifting gradient band, and the full-bleed swatch band. Everything else is ink on paper or paper on black. Depth comes from the ground changing, not from shadows.

Motion has two layers. The first is one-time reveals as things enter view: text rises and sharpens, the team tile wipes in and the two halves of Lando's LN4 logo slide together, and the speedmark sweeps in. The second is scroll-driven CSS: it lights words, drifts the rain, draws lines, eases the hero away, and fills a reading-progress line under the nav. Reduced motion and browsers without `animation-timeline` get the finished state, never an empty one.

**Key Characteristics:**
- Three section grounds: white, Apple grey, black. The nav follows them.
- One accent, papaya, used for actions and drawn lines.
- Mona Sans only, weights 500 to 700, with tight tracking on display sizes.
- Images break the grid: they bleed off a viewport edge or cross a section edge, and the copy alternates sides. The quiet sections near the end are centred.
- Real media only: the rain photograph (Pixabay), McLaren marks as vectors (Wikimedia Commons), and Lando's LN4 logo (supplied by the owner). All credited.
- One-time reveals plus scroll-driven CSS, whose resting state is always the finished state.

## Colors

Apple's neutrals with one McLaren orange: the palette is almost entirely greyscale so papaya reads as the signal.

### Primary
- **Papaya** (#ff8000): pill button fill, the Sochi trace and MS wave strokes, the trace's finish dot, focus rings, text selection, the swatch band's left half, and `theme-color` for Discord embeds. Only as a fill or stroke; it fails as small text on white.
- **Burnt Papaya** (#a94f00): papaya as text on light grounds (text links). This is the AA-safe papaya for white and grey.
- **Lit Papaya** (#ff9a3d): papaya as text on black (text links, trace annotations for the rain and the finish).

### Secondary
- **Ribbon Orange** (#f7862b): the MS awareness ribbon orange. It appears only in the swatch band, beside papaya. It is a brand fact, not a second accent.

### Neutral
- **Paper White** (#ffffff): the default page ground.
- **Apple Grey** (#f5f5f7): the quiet ground for the Patchr section and the footer. On black it is also the primary text colour (On-Dark).
- **True Black** (#000000): the cinematic ground for the Insko Bot and Formula 1 sections.
- **Ink** (#1d1d1f): primary text on light grounds, and text on papaya.
- **Ink Secondary** (#6e6e73): leads beneath headlines, secondary copy, fine print on light grounds.
- **On-Dark Secondary** (#a1a1a6): leads, axis labels and captions on black.
- **Hairline** (#d2d2d7): 1px dividers on light grounds (the hairline list, the footer rule, the wave baseline).
- **Hairline Dark** (#2c2c2e): 1px gridlines on black (the Sochi trace grid).
- **Tile** (#161617): a raised dark surface on black. Used for the McLaren / LN4 team tile, so it reads as an object rather than a hole.
- **Lando Volt** (#d2ff00): Lando Norris's brand colour. It appears only inside his LN4 mark, the same way the Insko icon keeps its own violet. It is never a page accent, text colour or fill.
- **Patchr Ink** (#17142B) and **Patchr Pink** (#FF5E8A): Patchr's own colours. Like Lando Volt, they appear only inside the Patchr mark (src/components/PatchrMark.astro, inlined from the Patchr brand kit), never as page accents.

### Named Rules
**The One Orange Rule.** Papaya is the only accent. It marks things you can act on, and lines that tell the story. Nothing decorative is papaya except the hero band and the swatch band, and each appears once.

**The Three Grounds Rule.** Every section sits on #ffffff, #f5f5f7 or #000000. Ground changes are the section breaks; no borders, cards or tinted panels stand in for them. The one exception is a photo band, where the photograph is the ground.

**The Papaya-As-Text Rule.** Text in papaya uses #a94f00 on light grounds and #ff9a3d on black. Raw #ff8000 is for fills and strokes only.

## Typography

**Display Font:** Mona Sans Variable (with -apple-system, BlinkMacSystemFont, Segoe UI, system-ui)
**Body Font:** Mona Sans Variable (same stack)

**Character:** One variable neo-grotesque doing everything, SF-like and neutral. Weight and tracking do the work a second family would do: 650 with tight negative tracking at display sizes, 500 for leads, 400 for body.

### Hierarchy
- **Display** (650, clamp(3.25rem, 10vw, 6rem), line-height 1, -0.04em): the hero name only. The 404 uses the same treatment one step down (clamp(3rem, 9vw, 5.5rem)).
- **Headline** (650, clamp(2.5rem, 6vw, 4.5rem), 1.05, -0.035em, balanced): one per section. Short declaratives ending in a full stop.
- **Statement** (650, clamp(2rem, 4.6vw, 3.5rem), 1.12, -0.035em): large standalone lines such as the scroll-lit statement, the quote, and the swatch line (these run between 3.5rem and 4rem at the top end). Max 18 to 24ch.
- **Title** (650, clamp(1.375rem, 2.4vw, 1.75rem), -0.025em): list item names and chart titles.
- **Lead** (500, clamp(1.1875rem, 1.9vw, 1.5rem), 1.4, -0.012em, pretty): the sentence under a headline, max 28 to 38ch, usually in the secondary colour. The hero lead runs slightly larger (up to 1.625rem).
- **Body** (400, clamp(1.0625rem, 1.3vw, 1.1875rem), 1.55): running text. The base body size is 1.0625rem.
- **Label** (400 to 600, 0.8125 to 0.875rem, line-height 1): nav links, chart axis and annotation labels, wave labels, swatch names. Sentence case, never uppercase.
- **Fine** (400, 0.8125rem, 1.45, secondary colour): photo credits and the footer.

### Named Rules
**The One Family Rule.** Mona Sans only. Hierarchy comes from size, weight (500, 560, 600, 650, 700) and tracking, never from a second typeface.

**The Tight Display Rule.** The bigger the type, the tighter the tracking: -0.04em at display, -0.035em at headline and statement, -0.025em at title, about -0.01em at body.

**The Sentence Case Rule.** No uppercase, no letter-spaced small labels, and no small line above a headline. A headline introduces itself.

## Layout

Content sits in `.container`: min(100% minus two gutters, 1080px), with a fluid gutter of clamp(1.25rem, 5vw, 3rem).

**Nothing Stays In Its Box.** Images never sit as centred boxes. Each one breaks a boundary tied to its story beat, and the copy alternates sides, so the page zigzags:
- The hero headline crosses the gradient band's slanted edge.
- The Insko icon sits right and crosses up from the black section into the white one above: a negative top margin of section-y plus 45% of the icon.
- The McLaren / LN4 team tile holds the right-hand grid line and bleeds off the right edge of the viewport, with the copy in a 24rem left column.
- Rain washes over the right end of the pinned Sochi chart.
- The rain panel bleeds off the left edge, with the copy in a 26rem right column.
- The speedmark sits large to the right of a left-aligned quote.
- The Patchr mark sits centred on the edge between the white MS section and the grey Patchr section. That section has no top padding; the mark rises half its own height into the section above.
- The swatch band runs full-bleed.

Bleeding stages use named grid lines (`full-start` / `text` / `photo-start` / `full-end`) so the image column runs to the viewport edge while the text stays on the container. Sections after the swatch (MS, Patchr) return to quiet centred stacks as the page's resolution. On phones every split stacks: headline, then the bleeding image, then the rest.

Centred sections are stacks (grid, justify-items centre, text centred) with a 1.5rem gap. Vertical padding comes from one fluid token, section-y (clamp(6rem, 13vw, 10rem)). Spacing uses a 0.5rem-based scale (0.5, 1, 1.5, 2, 3, 4, 6, 8rem). Rhythm inside a section is 1.5rem between headline, lead and actions, with 2 to 3rem before secondary notes and 6rem before lists or media.

Actions sit in a centred, wrapping row: pill first, text link second, 1rem by 2rem gap.

The sticky nav is 3rem tall; `scroll-padding-top` is 3.5rem so anchor jumps clear it. Breakpoints are content-driven: 36rem (hero band shortens, the hairline list stacks to one column) and 40rem (the chart thins its axis labels and drops optional annotations). Mobile keeps the same stack, just smaller type through the clamps.

## Elevation & Depth

Flat. There are no drop shadows anywhere. Depth comes from the ground changing (white to grey to black), from the translucent blurred nav, and from photographs. The only box-shadows are 1px or 4px spread rings with no offset and no blur, used as edges.

### Shadow Vocabulary
- **Icon edge** (`box-shadow: 0 0 0 1px rgb(255 255 255 / 0.14)`): a hairline around a dark app icon on black so its edge reads.
- **Knockout ring** (`box-shadow: 0 0 0 4px #000`): separates the trace's finish dot from the gridlines behind it.

### Named Rules
**The No-Shadow Rule.** Nothing floats. If something needs separating, change the ground, add a 1px hairline, or use a zero-offset ring.

## Shapes

Three curves. Media frames use a 28px radius on the corners that face the page. Corners that bleed off the viewport are square. Actions are full pills (980px). The app icon uses a squircle-like 27%. Focus rings are rounded to 6px. Everything else (sections, bands, lists) is square and edge to edge. Lists are separated by 1px hairlines, not boxed. Drawn lines use 3px round-capped, round-joined strokes.

## Components

### Buttons
Apple's pill, in papaya. Confident, and never more than one per action row.
- **Shape:** full pill (980px), at least 2.75rem tall.
- **Primary:** papaya fill, ink text, 600 weight, 1.0625rem, 0 1.375rem padding. The same on light and dark grounds.
- **Hover / Active:** fill lightens slightly to #ff9022 (200ms ease); press scales to 0.97 on the expo-out curve.
- **Focus:** 2px papaya outline, 3px offset.

### Text link with chevron
The secondary action, always beside or after a pill.
- **Style:** burnt papaya (#a94f00) on light grounds, lit papaya (#ff9a3d) on black. Weight 560, 1.0625rem, no underline at rest, 2.75rem tap height.
- **Glyph:** an inline Lucide chevron-right (18px, 2px stroke, currentColor) sits 0.2em after the label. External destinations use the Lucide external-link glyph (16px) instead.
- **Hover:** underline (1px, offset 0.2em).
- **Inline links** inside copy and credits inherit the text colour and keep the default underline.

### Navigation
A translucent Apple-style global bar that follows the ground beneath it.
- **Style:** sticky, 3rem tall, background rgb(251 251 253 / 0.72) with `backdrop-filter: saturate(180%) blur(20px)` and a 1px rgb(0 0 0 / 0.08) bottom rule. The name sits left (650, 1.0625rem, -0.02em). Section links sit right (0.875rem, ink at 80%, full ink on hover), each with a 2.75rem tap height.
- **Over black:** an IntersectionObserver on every `.dark` section sets `data-dark` when a dark section is under the bar. The bar then turns rgb(22 22 23 / 0.8), its rule becomes rgb(255 255 255 / 0.08), and the links go on-dark at 80%. The switch cross-fades over 300ms.
- **Mobile:** the same bar with the link gap tightened (clamp(1rem, 3vw, 2.25rem)). There is no hamburger menu.

### Hairline list
Replaces a card grid for short lists (the Patchr section).
- **Style:** max 48rem, centred. A 1px hairline sits above the first row and below each row. Each row is a two-column grid (1fr / 1.4fr, baseline-aligned) holding a Title-size name and secondary body text, with 1.5rem vertical padding. Below 36rem each row stacks.

### Scroll-lit statement
One large sentence whose words light up in order as it crosses the viewport. It uses Statement type, max 24ch. Each word is a span animated from #8e8e93 to the text colour on a named view timeline, staggered by index. #8e8e93 keeps unlit words at 3:1 on white. At rest (reduced motion, or no support) every word is fully lit.

### Pinned Sochi trace (signature)
A data chart that pins while it draws. The section is 240vh tall; the chart sticks under the nav (top 3rem, min-height 100vh minus 3rem). On black, it has 1px hair-dark gridlines, P1 to P8 axis labels in on-dark secondary with tabular numbers, and a 3px papaya polyline revealed by an animated `clip-path` inset. Annotations fade in at the lap they describe: white 560-weight labels, lit papaya for the rain and the finish. A papaya dot with a 4px black knockout ring marks the finish. A visually hidden figcaption carries the full story in prose. The data is real (Jolpica lap positions).

### Photo frame and photo band
- **Team tile (F1, signature):** a 4:3 Tile-coloured (#161617) panel. It runs from `photo-start` to the right viewport edge, with a 28px radius on the left corners only. It holds the McLaren wordmark (top left, on-dark, papaya speedmark), Lando's LN4 logo in Lando Volt (66% of the height, anchored right), and "Lando Norris" (bottom left, 650). It wipes in from the right edge (clip-path). Then the logo's two interlocking pieces slide in along its slant and lock: the L up from the bottom left (translate -7% 26%), and the 4 down from the top right (7% -26%) 100ms later, over 1.1s. The wordmark and name follow at 1.15s. The parent section keeps `overflow-x: clip`, not `hidden`, so motion can't widen the page and the pinned trace stays sticky. The logo credits sit beneath as a left-aligned 0.8125rem caption.
- **Bleeding panel (rain):** runs from the left viewport edge to `panel-end`, min(42rem, 78vh) tall on a grey section, with a 28px radius on the right corners only. The image is 118% tall and drifts down 15% across the panel's view range, like falling rain. On phones it is 4:3.
- **Chart rain:** the same rain photo is inverted, grayscaled and crushed toward black, so its drops read as light specks. It sits behind the pinned chart at 0.55 opacity, masked to the right 40% of the chart. It fades in over contain 64% to 76%, just before lap 51.

### Swatch band
The page's one drenched moment: full-bleed, split 50/50 papaya and ribbon orange, with each half's name in small 600-weight ink at its outer bottom corner. A Statement-size line in ink is centred across both halves. It appears once.

### Hero gradient band
The Stripe move, used once: a band skewed -7deg, filling the top of the hero (clamp(17rem, 50vh, 30rem)). It has a #ffb86b base and three large radial blobs (#ff8000, #ff5a36, #ffd9ae) drifting on 22 to 30s alternate loops, animated by transform only. The display name overlaps its slanted lower edge. The hero content fades and rises 1.25rem on load (1100ms expo-out, staggered 120ms).

### Wave line
An illustrative SVG (not data): a 3px papaya path that rises for relapses and settles for remission, over a dashed 1px hairline baseline (3 6 dash). Sentence-case labels in ink secondary sit above or below. It draws left to right on a view() timeline.

### Motion grammar
- **Easing:** `--ease-out` cubic-bezier(0.16, 1, 0.3, 1) for entrances and presses; plain `ease` for colour transitions (200 to 300ms); `linear` for every scroll-driven animation.
- **Scroll-driven, and gated:** every scroll-linked animation sits inside `@supports (animation-timeline: view())` and `@media (prefers-reduced-motion: no-preference)`. Keyframes are written as `from` only (or with a finished `to`), so the unanimated state is always the finished state.
- **Longhands only:** write `animation-name`, `animation-timing-function`, `animation-fill-mode`, `animation-timeline`, `animation-range` as separate declarations. Never use the `animation` shorthand for a scroll-driven animation. The build's CSS minifier folds `animation-timeline` into the shorthand, and Chrome then drops the whole declaration, so the animation silently never runs.
- **Page navigation:** cross-document view transitions (`@view-transition { navigation: auto }`).
- **Patchr mark:** once the tile has popped in, its changelog lines write themselves: the stem drops (scaleY from 0, 250ms delay), the white bars draw out from the left (450ms and 560ms), and the pink bar, the newest change, arrives last (720ms, over 950ms). All on --ease-out. Reduced motion shows the finished mark.
- **Reveals (`data-reveal`):** one-time, triggered by an IntersectionObserver in Base.astro (threshold 0.15, bottom margin -8%).
  - Content is hidden only under `html.js`, set by an inline head script, so a failed script never hides the page.
  - Variants: default (opacity, 1.5rem rise and 8px blur to sharp, over 700 to 900ms), `pop` (scale 0.88), `sweep` (35% slide and -14deg), `wipe-left` and `panel` (clip-path wipes).
  - Siblings inside `data-reveal-group` stagger 90ms each, capped at 5 steps.
  - Clip-based variants start fully clipped, and the observer treats fully clipped elements as invisible, so the script watches their parent instead.
  - Reduced motion keeps only a 400ms opacity fade.
- **Feedback:** text links draw a 1px underline in from the left (300ms) and nudge their chevron 3px. Nav links draw an underline on hover (250ms). The pill lightens and presses to 0.97.
- **Ambient scroll:** a 2px papaya reading-progress line along the nav's bottom edge (`scroll(root)`), and the hero content easing up and dimming as it exits.

## Do's and Don'ts

### Do:
- **Do** put every section on #ffffff, #f5f5f7 or #000000, and add the `.dark` class to black sections so the nav turns dark over them.
- **Do** pair one papaya pill with one chevron text link in a centred action row.
- **Do** use #a94f00 for papaya text on light grounds and #ff9a3d on black.
- **Do** write scroll-driven animations as longhands inside `@supports (animation-timeline: view())` and `prefers-reduced-motion: no-preference`, with the finished state as the default.
- **Do** source photographs from Pixabay through `scripts/fetch-pixabay.mjs` (IDs in `scripts/pixabay.json`). Commit them to `src/assets/pixabay/`, serve them through Astro's `Picture` (avif and webp at 640, 960 and 1280 widths), and credit each one both under the image and in the footer from `credits.json`.
- **Do** inline team and driver marks as SVG Astro components that colour through `currentColor`, and record where each came from in the component. The McLaren marks come from Wikimedia Commons; the LN4 logo was supplied by the owner. Say on the page who each mark belongs to. Use the exact path data from the source file, never retyped by hand.
- **Do** keep images at or below the 1280px the standard Pixabay key returns. Size frames and `sizes` attributes so a 1280px source is not stretched past it.
- **Do** keep tap targets at 2.75rem or more, and give every drawn chart a prose equivalent for screen readers.

### Don't:
- **Don't** use drop shadows, cards or tinted panels. Separate with the ground, a 1px hairline or a zero-offset ring.
- **Don't** add a second accent colour. Ribbon orange lives only in the swatch band.
- **Don't** use raw #ff8000 for small text on white or grey.
- **Don't** use the `animation` shorthand for a scroll-driven animation.
- **Don't** hotlink Pixabay images or ship the API key. The key stays in `.env`.
- **Don't** set labels in uppercase or tracked small caps, and don't put a small label line above a headline.
- **Don't** repeat the hero gradient band or the swatch band. Each is used once.
- **Don't** introduce a second typeface or an icon font. Icons are inline Lucide SVGs.

# Design System: Patchr site (/patchr/)

Scope: Patchr's own product site. That is `src/pages/patchr/` (the home page, privacy and terms), `src/layouts/PatchrBase.astro` and `PatchrLegal.astro`, `src/components/patchr/`, and `src/styles/patchr.css`. It is a second, separate visual world in this repo. Its tokens are the `patchr-` keys in the frontmatter. Nothing here applies to the personal site, and nothing in the personal system above applies here, except the shared reveal system (`src/styles/reveal.css`, `src/scripts/reveal.ts`), which each world feeds its own `--radius` and `--ease-out`.

## Overview

**Creative North Star: "The Bot Page, Finished"**

This is the Discord-bot landing page that server owners already know from MEE6, YAGPDB and Carl.gg: a hero with Add to Discord, feature rows proven by Discord product shots, how it works, limits, commands, FAQ, one drenched call-to-action band, and a legal footer. It is built with the personal site's Apple and Stripe finish, in Patchr's own brand kit (`brand/BRAND.md`). The page starts dark, because visitors arrive from Discord's dark UI. It opens on deep ink, moves through light white and mist feature sections, returns to deep ink for How it works, and ends on the pink band.

The proof is the product itself. Every feature is shown as a Discord screen drawn in HTML, using the layout and strings of Patchr's real renderer. The screens bleed off the viewport edge, opposite the copy. Discord's own greys stay inside those product shots, the way a phone keeps its own screen colours inside a product photo. Pink is the one action colour, and in the brand it means *the newest change*.

**Key Characteristics:**
- Deep ink for dark sections, white or mist for light ones. The nav follows the ground beneath it.
- One accent, pink, used for actions, the newest change and drawn lines. Ink text sits on it.
- Inter Variable only: 800 for display and headlines with tight tracking, 500 for leads.
- Discord product shots that bleed off alternating viewport edges, rounded only on their inner corners.
- Add to Discord is the live pink pill, opening Patchr's invite in a new tab, with the support server beside it.
- One-time reveals, a floating 3D hero tile, and scroll-drawn pink lines, all with a finished resting state.

## Colors

Violet-tinted inks and a lavender mist, with one hot pink. The palette comes straight from the brand kit.

### Primary
- **Patchr Pink** (patchr-pink): the action colour and the newest change. It fills the pill buttons (Add to Discord) and the CTA band. It is also the stroke of the How it works flow lines, the reading-progress line, focus rings, text selection, the check icons on dark, and text links on dark. The `theme-color` is pink too, so Discord paints a shared link's embed stripe pink. It hovers to patchr-pink-hover.
- **Deep Pink** (patchr-pink-text): pink as text or small icons on white and mist. Used for text links, slash-command chips, check icons, FAQ plus icons and hovers, spec units, legal links and list markers. About 5:1 on mist.
- **Pink Blush** (patchr-pink-soft): text on the translucent pink tint on dark grounds, used for slash-command chips.

### Tertiary
- **Fix Mint** (patchr-mint) and **Change Lilac** (patchr-lilac): the colours of the Fix and Change badges. They appear only inside those badge images, the brand kit's PNGs that Patchr uploads to Discord as emojis. No CSS on the page uses them.

### Neutral
- **Deep Ink** (patchr-deep): the default page ground: the hero, How it works and the footer. Also the nav glass tint.
- **Patchr Ink** (patchr-ink): primary text on light grounds and on pink, the phone menu's panel, and the ink button on the pink band.
- **Mist** (patchr-mist): the quiet light ground, alternating with white. Also the ground for the legal pages and the light nav glass.
- **White** (patchr-white): the other light ground, and primary text on dark.
- **Haze** (patchr-haze): secondary text on dark (leads, facts, footer, flow labels).
- **Muted** (patchr-muted): secondary text on light.
- **Line Light** (patchr-line-light) and **Line Dark** (patchr-line-dark): 1px hairlines on light and dark grounds (spec rows, FAQ, facts strip, How it works points, footer).
- **Discord greys** (patchr-dc-*): Discord's own UI colours, used only inside the product shots. These are the channel ground, the post container, inputs, text, headings, muted text, links, blurple, separators and buttons.

### Named Rules
**The Ink-On-Pink Rule.** Pink is the one action colour. Whatever sits on pink is set in Patchr Ink, never white. White on pink is under 3:1, and ink on pink is above 6:1.

**The Deep-Pink Text Rule.** Pink as text on white or mist uses patchr-pink-text. Raw pink is for fills, strokes and text on dark.

**The Badge-Only Rule.** Mint and lilac exist only in the Fix and Change badges. They are never a text colour, fill or accent on the page.

**The Screen-Colours Rule.** Discord's greys, and the colours of the GitHub release card, appear only inside product shots of those UIs. That means the Discord screens, the /patch modal floating off the second screen, and the source and output cards in How it works. Around them the page stays in Patchr's own palette.

## Typography

**Display Font:** Inter Variable, with optical sizing (-apple-system, BlinkMacSystemFont, Segoe UI and system-ui as fallbacks)
**Body Font:** Inter Variable (same stack)
**Label/Mono Font:** the system monospace stack (ui-monospace, SF Mono, Cascadia Mono, Consolas), for slash commands and Markdown samples

**Character:** The brand kit's pairing, done with one variable family: ExtraBold 800 with tight tracking for the big lines, Medium 500 for leads and interface text. Body text is tracked slightly tight at -0.011em, with `font-optical-sizing: auto`.

### Hierarchy
- **Display** (patchr-display): the hero line only, max 11ch, balanced.
- **Figure** (patchr-figure): the big numbers in Good to know, with tabular figures. A unit in Deep Pink sits beside each one (700, up to 1.375rem).
- **Headline** (patchr-headline): one per section. Short declaratives, balanced. Legal page titles use the same treatment (800, clamp(2.5rem, 6vw, 4rem)), and legal h2s use 750.
- **Title** (patchr-title): FAQ questions, spec names and How it works points.
- **Lead** (patchr-lead): the sentence under a headline, usually in the secondary colour, max 30 to 38ch.
- **Body** (patchr-body): running text. The `.body` class opens to 1.6 line height, and legal text uses 1.65.
- **Label** (patchr-label): nav links, the facts strip and footer links. Sentence case.
- **Button** (patchr-button): pill labels.
- **Fine** (patchr-fine): example captions, the hero note and the footer line.

### Named Rules
**The One Family Rule.** Inter only (plus system mono for commands). Hierarchy comes from size, weight (500, 550, 560, 600, 620, 650, 700, 750, 800) and tracking.

**The Sentence Case Rule.** No uppercase labels and no small line above a headline. The one uppercase string is Discord's own APP tag, inside a product shot.

## Layout

Content sits in a 1120px container (patchr-container) with the same fluid gutter and 0.5rem-based spacing scale (s1 to s16) as the personal site. Sections pad by patchr-section-y. The sticky nav is 3.75rem tall, and `scroll-padding-top` clears it by 1rem.

**Feature rows.** Copy sits in a text column (patchr-text-column, 27rem; 33rem for Commands), with a 4rem gap. The Discord screen runs from its grid line to the viewport edge on the other side. It is built on named grid lines (`full-start` / `text` / `shot-start` or `shot-end` / `full-end`) inside a section with `overflow-x: clip`. The three feature rows alternate: screen right, screen left, screen right. The Commands row bleeds right. On the left-bleeding row, the /patch form floats across the screen's inner edge toward the copy. An "Example post" caption in fine type sits under each screen.

**Hero.** A 1.08fr / 0.92fr grid. The brand kit's 3D tile render is drawn 250% as wide as its column, pushed toward the right edge, and radially masked into the deep ink, so the studio light becomes the hero's glow. A facts strip under a dark hairline replaces server counts.

**Grounds in order.** Hero (deep ink), three feature rows (white, mist, white), How it works (deep ink), Good to know (mist), Commands (white), FAQ (mist), the pink band, then the footer (deep ink). Light sections carry `.light` (plus `.mist`), and the nav follows them.

**Breakpoints.** 52rem is the main one: the hero, rows, specs and FAQ stack; the nav links move into a menu; and the How it works stage becomes a column joined by one pink line. Below 52rem each row stacks as text, then the screen, then the caption, and the /patch form overlaps the bottom of its screen. 26rem shrinks the nav's Add to Discord to the Discord glyph and the Soon tag. 68rem sets How it works points in two columns. The footer regrids at 48rem and 30rem.

**How it works stage.** A fixed-ratio (1000 / 440) container-query stage. The nodes sit at set percentages so that the SVG lines meet them exactly, and node type scales with `cqi`.

## Elevation & Depth

Mostly flat, with depth from ground changes, the translucent blurred nav, and the product shots themselves. Resting surfaces carry at most a zero-offset ring. Soft shadows appear only on the two things that genuinely float over other content.

### Shadow Vocabulary
- **Floating modal** (`box-shadow: 0 0 0 1px rgb(255 255 255 / 0.06), 0 1.5rem 3rem -0.5rem rgb(14 12 26 / 0.45)`): the /patch form crossing its screen's edge, tilted 2deg.
- **Menu panel** (`box-shadow: 0 0 0 1px rgb(255 255 255 / 0.08), 0 1rem 2rem -0.5rem rgb(0 0 0 / 0.5)`): the phone nav menu.
- **Hub glow** (`filter: drop-shadow(0 0.75rem 1.5rem rgb(255 94 138 / 0.25))`): the Patchr mark at the centre of How it works. Used once.
- **Edge rings** (inset or zero-offset 1px rings): the post container (white at 4%) and the slash menu (black at 25%).
- **Nav glass** (`backdrop-filter: saturate(180%) blur(20px)`): deep ink at 72%, or mist at 78% over light sections, with a 1px rule.

### Named Rules
**The Floating Layer Rule.** Only a layer that floats over other content gets a soft shadow, and it always comes with a 1px ring. Everything at rest is flat.

## Shapes

Actions are full pills (patchr-pill). Discord screens use patchr-screen on the corners that face the page. The corners that bleed off the viewport stay square. The phone menu panel uses patchr-panel, with patchr-panel-item rows. Focus rings round to patchr-focus, and slash-command chips to patchr-chip. Inside product shots, radii follow Discord in em (0.5em for posts, buttons and menus, 0.75em for the modal, circles for avatars). The Patchr tile is the brand kit's superellipse, never redrawn. Flow lines are 3px round-capped strokes. Lists are ruled with 1px hairlines, never boxed.

## Components

### Buttons
Pills, one filled action per row, and the secondary action beside it outlined.
- **Shape:** full pill, 3rem tall (2.5rem for the small nav version), with a gap of 0.6rem between glyph and label.
- **Primary (patchr-button-pink):** pink fill, ink text. It hovers to patchr-pink-hover, and presses to 0.97 on --ease-out. Used for Add to Discord.
- **Ghost (patchr-button-ghost):** a 1px white border at 22% on dark (ink at 20% on light). On hover the border strengthens and a faint fill appears. Used for View code.
- **Ink (patchr-button-ink) and Ink Ghost (patchr-button-ink-ghost):** used only on the pink band, where pink buttons would vanish. Ink fill with white text for Add to Discord, hovering to #2a2546. An ink outline at 35% for Join the support server.
- **Focus:** a 2px pink outline with a 3px offset, rounded to 8px.

### Add to Discord (signature)
- **Style:** the primary pink pill holding the Discord glyph and "Add to Discord" (src/components/patchr/AddToDiscord.astro, `tone="ink"` on the pink band). It appears in the nav (small), the hero and the pink band.
- **Behaviour:** a real link to Patchr's invite (src/data/patchr/links.ts, which also holds the support server and repo URLs). It opens in a new tab and says so to screen readers.
- **Narrow phones (under 24rem):** the nav version reads "Add" beside the glyph. The full label stays for screen readers.

### Text link
Pink on dark and Deep Pink on light, weight 620, with a 2.75rem tap height. On hover a 1px underline draws in from the left (300ms), and the chevron or external glyph nudges 2px.

### Slash-command chip
A command set the way Discord shows it: system mono at 0.9em, on a 14% pink tint with Pink Blush text (on light, an 8% deep-pink tint with Deep Pink text), with 6px corners. It closes each feature row's copy.

### Navigation
- **Style:** sticky, 3.75rem, deep-ink glass. The logo sits left (1.75rem tall, its ink tile given a 4px haze hairline so it doesn't vanish into the bar). Section links follow (0.9375rem, 500, white at 72%, full white on hover, with an underline drawing in), then the small Add to Discord.
- **Light follow:** an IntersectionObserver on `.light` sections sets `data-light`. The bar then turns to mist glass, ink links and a deep-pink invite, cross-fading over 300ms.
- **Reading progress:** a 2px pink line along the bar's bottom edge, scaled by `scroll(root block)`.
- **Phones (under 52rem):** the links move into a `details` menu (a 2.75rem round trigger, and an ink panel with 2.75rem rows plus View code). It closes on link choice, outside tap or Escape. The invite stays in the bar.

### Discord screen (signature)
A Discord channel drawn as a product shot. It has a channel bar (hash or megaphone icon, channel name, an optional blurple Follow button) and a message body on the Discord channel ground. Everything inside is sized in em from one font size (clamp(0.8125rem, 1.25vw, 1rem)), so the whole screen scales as one. Messages show the Patchr avatar, the name, Discord's APP tag and a timestamp. The announcement screen masks its older post out at the top so it reads as a history.

### Patch card
A Patchr post, built the way Patchr's renderer lays it out. It has an optional role ping, then one container (patchr-patch-card) holding the project name in Discord link blue, "version · title", a separator, notes with badge headings, a footer line with the Patchr mark, and grey link buttons. **The accent stripe:** a 0.25em (4px at full size) pink bar down the container's left edge. It is the one sanctioned side stripe, because it reproduces Discord's Components V2 `accent_color`, which Patchr sets on every real post. The detector's ignore-value for it lives in `.impeccable/config.json`.

### Change badges
The brand kit's New (pink), Fix (mint) and Change (lilac) badge images, set inline at 1.375em before note headings, just as Patchr adds them in Discord.

### How it works flow (signature)
A GitHub release card (in GitHub's own dark colours) and a /patch mini modal on the left. Two pink lines run from them into the Patchr mark at the centre, and one line runs out to a stack of two identical posts. The labels are in haze at 600. On phones it becomes a column threaded by one 3px pink line.

### Specs and FAQ
Hairline-ruled rows on mist. Specs pair a Figure-size number and a Deep Pink unit with a title and a secondary sentence. FAQ items are `details` rows with a Title-size question and a Deep Pink plus icon that turns 45deg when open. Answers open smoothly where `interpolate-size` is supported.

### CTA band
**The One Drench Rule.** The pink band is the page's one drenched moment: full-bleed pink, ink text, the Patchr mark popping in, a centred headline (max 16ch), and the Add to Discord and ink View code pair. Text selection inverts to ink with pink text. It appears once, just before the footer.

### Footer
Deep ink under a dark hairline. The logo and tagline sit beside three link columns (Patchr, Legal, Source), with a fine line for copyright and the trademark notice.

### External links
Every link that leaves the site opens in a new tab (`target="_blank" rel="noopener"`), carries the external glyph where it is a standalone action, and ends with a visually hidden "(opens in a new tab)". The legal layout adds all of this by script, because the policies are Markdown.

### Icons
Inline SVG only. The stroke icons are Lucide. The Discord and GitHub glyphs are Simple Icons, with their path data copied. The Patchr mark and logo are inlined from the brand kit with their path data copied, and their colours stay inside the mark.

### Motion grammar
- **Easing:** --ease-out, cubic-bezier(0.16, 1, 0.3, 1), for entrances, presses and draws. Plain `ease` for colour (200 to 300ms). `linear` for anything scroll-driven.
- **Shared reveals:** the personal site's `data-reveal` system (see Motion grammar above), run by `src/scripts/reveal.ts` from PatchrBase with Patchr's 24px radius. Right-bleeding screens use `panel`, the left one uses `wipe-left`, and the /patch form, the hub mark and the CTA mark use `pop`. Copy stacks stagger through `data-reveal-group`.
- **Hero:** the copy fades and rises 1.25rem (1100ms, staggered 120, 240 and 320ms). The tile arrives from scale 0.9 and a 12px blur (1600ms, after 150ms), then floats on a 7s alternating loop (±1.2% and ±1.2deg). As the hero scrolls out, it dims to 0.25 and lifts 2.5rem on a `view()` exit range.
- **Post children:** once a screen has wiped in, the post's parts rise 0.5em in order (450, 520, 590, 660, then 780ms), so it reads as the newest post writing itself.
- **Flow lines:** drawn by `stroke-dashoffset` on a named view timeline (`--flow`). The two lines in draw over entry 60% to contain 25%, and the line out over contain 20% to 45%. They are finished by default.
- **Patchr mark writing:** in the hub, the stem drops (250ms), the white bars draw out (450ms), and the pink bar arrives last (720ms, over 950ms).
- **Smaller moves:** a blinking caret in the slash composer, the FAQ plus turning, and the nav underline and progress line.
- **Gating and the minifier pitfall:** every scroll-driven animation sits inside `@supports (animation-timeline: …)` and `prefers-reduced-motion: no-preference`, written as longhands (`animation-name`, `-timing-function`, `-fill-mode`, `-timeline`, `-range`). Astro's CSS minifier folds `animation-timeline` into the `animation` shorthand, and Chrome then drops the whole declaration. Time-based loops may use the shorthand. Reduced motion gets the finished state: reveals fade only, posts and the mark show complete, and there is no float or caret.
- **Page navigation:** cross-document view transitions.

## Do's and Don'ts

### Do:
- **Do** put dark sections on patchr-deep, and light sections on white or mist with `.light` (and `.mist`), so the nav follows the ground.
- **Do** set ink text on pink, Deep Pink for pink text on light grounds, and raw pink only for fills, strokes and text on dark.
- **Do** lead every action row with Add to Discord, and link the support server wherever help is offered. Take all three Patchr URLs from src/data/patchr/links.ts.
- **Do** prove features with Discord screens built from Patchr's real renderer layout and strings, bleeding off the edge opposite the copy, rounded only on the inner corners, and captioned as examples.
- **Do** open external links in a new tab with a visually hidden "(opens in a new tab)".
- **Do** write scroll-driven animation as longhands inside both gates, with the finished state as the default.
- **Do** keep tap targets at 2.75rem or more.

### Don't:
- **Don't** put white text on pink.
- **Don't** use mint or lilac outside the Fix and Change badges, or Discord's greys outside a product shot.
- **Don't** bring the personal site's papaya or Mona Sans onto /patchr/, or Patchr's pink and Inter onto the personal pages.
- **Don't** add a second drenched band. The pink CTA band appears once.
- **Don't** add side stripes to cards. The Patch card's accent stripe is the one exception, because it reproduces Discord.
- **Don't** give resting surfaces a drop shadow. Only floating layers get one, always with a 1px ring.
- **Don't** use uppercase or tracked labels, or a small line above a headline.
- **Don't** recolour the Patchr mark's bars or set the wordmark in another font.
